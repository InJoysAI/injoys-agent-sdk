# 设计系统规范模板 (Design System Template)

> **Metadata**
> - **Source**: `{{UI 规范文档路径}}`
> - **Generated At**: `{{生成时间 YYYY-MM-DD HH:mm}}`
> - **Generator**: `Context-Agent v1.0`

---

## 颜色系统

### 主色调

| 名称 | 值 | CSS 变量 | 用途 |
|------|-----|---------|------|
| Primary | `{{色值}}` | `--color-primary` | 主要按钮、链接 |
| Primary Hover | `{{色值}}` | `--color-primary-hover` | 悬停状态 |

### 中性色

| 名称 | 值 | CSS 变量 | 用途 |
|------|-----|---------|------|
| Gray 50 | `{{色值}}` | `--color-gray-50` | 页面背景 |
| Gray 900 | `{{色值}}` | `--color-gray-900` | 主要文本 |

### 语义色

| 名称 | 值 | 用途 |
|------|-----|------|
| Success | `{{色值}}` | 成功状态 |
| Error | `{{色值}}` | 错误状态 |

---

## 字体规范

### 字体族

```css
--font-sans: '{{字体名}}', sans-serif;
--font-mono: '{{等宽字体名}}', monospace;
```

### 字号

| 名称 | 大小 | 行高 | 用途 |
|------|------|------|------|
| text-sm | {{大小}} | {{行高}} | 正文小 |
| text-base | {{大小}} | {{行高}} | 正文 |
| text-xl | {{大小}} | {{行高}} | 标题 |

---

## 间距系统

| 名称 | 值 | 用途 |
|------|-----|------|
| spacing-1 | {{值}} | 紧凑间距 |
| spacing-4 | {{值}} | 元素间距 |
| spacing-8 | {{值}} | 大区块间距 |

---

## 组件规范

### Button

```html
<!-- Primary -->
<button class="{{主按钮样式类}}">
  按钮文字
</button>

<!-- Secondary -->
<button class="{{次按钮样式类}}">
  按钮文字
</button>
```

### Input

```html
<input 
  type="text" 
  class="{{输入框样式类}}"
  placeholder="输入内容"
/>
```

---

## AI 引用指南

当 AI 生成前端组件时：
1. 使用上述定义的颜色变量，不要硬编码颜色值
2. 遵循间距系统
3. 复用组件样式类

