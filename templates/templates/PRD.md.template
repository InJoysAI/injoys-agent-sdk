# 📁 项目代号：{项目名称}

**文档类型：** 产品需求文档 (PRD)
**版本：** {版本号，如 MVP v1.0 / v1.1 / v2.0}
**作者：** {作者姓名}
**创建日期：** {YYYY-MM-DD}
**最后更新：** {YYYY-MM-DD}
**文档状态：** {草稿 / 评审中 / 已批准 / 已归档}
**目标平台：** {如 Windows 桌面端 (Tauri)、macOS、iOS、Web 等}
**核心商业逻辑：** {一句话概括盈利模式，如"免费增值"、"订阅制"、"激活码付费解锁"等}

---

## 修订记录

| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| {v0.1} | {YYYY-MM-DD} | {姓名} | {初稿创建} |
| | | | |

---

## 1. 产品概述 (Product Overview)

### 1.1 背景与机会

> 描述产品诞生的市场环境、用户痛点和商业机会。

- **市场现状：** {当前市场中存在的问题或未被满足的需求}
- **目标用户痛点：** {用户目前面临的核心痛点，1-3 个}
- **竞品分析摘要：** {主要竞品及其不足，为什么现有方案不够好}
- **切入点：** {本产品的差异化切入角度}

### 1.2 核心价值主张

> 用 3-5 个要点说明产品为用户提供的核心价值。每个要点一句话。

- **{价值点1关键词}：** {一句话描述}
- **{价值点2关键词}：** {一句话描述}
- **{价值点3关键词}：** {一句话描述}

### 1.3 产品目标与成功指标

> 定义阶段性目标和可量化的成功标准。

| 目标类型 | 指标 | 目标值 | 时间窗口 |
|----------|------|--------|----------|
| 用户增长 | {如 DAU、注册量} | {目标数值} | {如 上线后3个月} |
| 用户留存 | {如 次日留存率} | {目标数值} | |
| 商业指标 | {如 付费转化率} | {目标数值} | |
| 体验指标 | {如 NPS 分数} | {目标数值} | |

### 1.4 术语约定

> 列出本文档中使用的专有名词及其定义，保证团队理解一致。

| 术语 | 定义 | 备注 |
|------|------|------|
| {术语A} | {定义} | {如：面向用户展示时使用"XX"替代} |
| {术语B} | {定义} | |

---

## 2. 用户角色与流程 (User Roles & Flow)

### 2.1 用户角色

> 定义产品涉及的所有用户角色及其权限层级。

| 角色名称 | 描述 | 核心权限 |
|----------|------|----------|
| {角色1，如"普通用户"} | {角色描述} | {角色可执行的操作} |
| {角色2，如"付费用户"} | {角色描述} | {角色可执行的操作} |
| {角色3，如"管理员"} | {角色描述} | {角色可执行的操作} |

### 2.2 核心用户路径

> 用编号步骤描述用户从进入到完成核心任务的主路径。

1. **{步骤名称}：** {用户行为描述}
2. **{步骤名称}：** {用户行为描述}
3. **{步骤名称}：** {用户行为描述}
4. **{步骤名称}：** {用户行为描述}
5. **{步骤名称}：** {用户行为描述}

### 2.3 关键用户场景

> 补充非主路径的重要场景（异常流、边界场景等）。

- **场景A — {场景名称}：** {当 XX 发生时，系统应 XX}
- **场景B — {场景名称}：** {当 XX 发生时，系统应 XX}

---

## 3. 功能需求说明 (Functional Requirements)

> **优先级约定：**
> - **P0（核心）：** MVP 必须包含，缺失则产品不可用
> - **P1（重要）：** 显著提升体验或具备战略价值，MVP 后首批迭代
> - **P2（增强）：** 锦上添花，按资源情况排期

---

### 3.1 {模块名称} (P0 - 核心)

> 简要描述此模块的职责与边界。

#### 3.1.1 {子功能名称}

- **功能描述：** {做什么}
- **交互要求：** {前端展示与交互细节}
- **逻辑要求：** {业务规则和约束}
- **验收标准：**
  - [ ] {可验证的标准1}
  - [ ] {可验证的标准2}

#### 3.1.2 {子功能名称}

- **功能描述：** {做什么}
- **输入/输出：**
  - 输入：{用户输入的内容/格式/限制}
  - 输出：{系统返回的结果/格式}
- **逻辑要求：** {业务规则和约束}
- **错误处理：**
  - {错误场景1} → {处理方式与用户提示}
  - {错误场景2} → {处理方式与用户提示}

---

### 3.2 {模块名称} (P0 - 核心)

> 同上格式，按模块拆分描述。

---

### 3.3 {模块名称} (P1 - 重要)

> 同上格式。

---

### 3.4 {模块名称} (P2 - 增强)

> 同上格式。

---

## 4. 非功能性需求 (Non-Functional Requirements)

### 4.1 安全性 (Security)

| 需求项 | 描述 | 目标 |
|--------|------|------|
| {如 数据加密} | {具体要求} | {如 AES-256} |
| {如 接口防护} | {具体要求} | {如 无全局密钥暴露} |
| {如 代码保护} | {具体要求} | {如 核心逻辑服务端隔离} |

### 4.2 性能 (Performance)

| 指标 | 目标 | 度量方式 |
|------|------|----------|
| 冷启动时间 | {如 ≤ 2秒} | {从点击图标到首屏可交互} |
| 核心操作响应 | {如 ≤ 500ms} | {从点击到界面反馈} |
| 安装包体积 | {如 ≤ 15MB} | {安装包文件大小} |
| 内存占用 | {如 ≤ 200MB} | {运行时峰值} |

### 4.3 兼容性 (Compatibility)

| 平台/环境 | 最低版本 | 优先级 |
|-----------|---------|--------|
| {如 Windows} | {如 Windows 10} | {P0} |
| {如 macOS} | {如 macOS 12} | {P1} |
| {如 分辨率} | {如 1366×768 ~ 4K} | {P0} |

### 4.4 可用性 (Usability)

- {如：核心任务完成步骤 ≤ 3 步}
- {如：新用户无需培训即可完成首次使用}
- {如：关键错误需在 3 秒内给出用户可理解的提示}

### 4.5 可维护性 (Maintainability)

- {如：支持远程配置热更新，无需发版修改运营文案}
- {如：日志系统支持远程收集关键错误}

---

## 5. UI/UX 设计规范 (UI/UX Guidelines)

### 5.1 视觉风格

- **关键词：** {3-5 个形容词，如"亲和力、童真、简洁"}
- **配色方案：**
  - 背景色：{如 暖色调 / 米白 / 深色}
  - 主色：{如 橙色 #FF6B35 / 天蓝 #4A90D9}
  - 辅助色：{如 XX}
  - 强调色：{如 XX}
- **圆角规范：** {如 按钮 12px、卡片 16px}
- **字体：** {如 系统默认 / 指定字体族}
- **图标风格：** {如 线性 / 填充 / 双色调}

### 5.2 交互原则

> 列出影响全局交互设计的核心原则。

- {原则1，如"单页应用，避免多级跳转"}
- {原则2，如"去专业化，用用户能理解的语言替代技术术语"}
- {原则3，如"操作即反馈，所有操作需在 300ms 内给出视觉反馈"}

### 5.3 关键页面/状态清单

> 列出需要设计的核心页面和状态，供设计师参考。

| 页面/状态 | 描述 | 优先级 |
|----------|------|--------|
| {如 首次启动引导} | {页面简述} | P0 |
| {如 主操作界面} | {页面简述} | P0 |
| {如 结果展示页} | {页面简述} | P0 |
| {如 空状态} | {页面简述} | P1 |
| {如 错误状态} | {页面简述} | P0 |

---

## 6. 商业运营需求 (Business & Operational Requirements)

### 6.1 运营位与引流

- **位置：** {如 设置页 / 底部状态栏 / 结果页}
- **能力要求：**
  - {如 支持远程配置文案更新}
  - {如 支持外链跳转（受控白名单）}
  - {如 仅用户主动点击触发，禁止自动跳转}

### 6.2 计费与付费

- **计费模型：** {如 按次计费 / 包月订阅 / 一次性买断}
- **额度耗尽处理：** {如 弹窗提示 + 引导购买}
- **退款/补偿策略：** {如 操作失败自动退还额度}

### 6.3 数据与隐私

- **数据收集范围：** {如 仅收集匿名使用统计}
- **用户隐私策略：** {如 无需注册登录，本地存储优先}
- **合规要求：** {如 符合 GDPR / 个人信息保护法}

---

## 7. 技术约束与依赖 (Technical Constraints)

> 列出对产品实现有影响的技术前提和外部依赖。

| 约束/依赖 | 说明 | 影响范围 |
|-----------|------|----------|
| {如 Tauri 框架} | {版本及能力限制} | {全局} |
| {如 后端 API} | {第三方服务/自建服务} | {生成模块} |
| {如 网络环境} | {如 需要互联网连接} | {核心功能} |

---

## 8. 里程碑与排期 (Milestones & Timeline)

| 里程碑 | 内容范围 | 预计日期 | 状态 |
|--------|---------|----------|------|
| M0 — 需求确认 | PRD 评审通过 | {YYYY-MM-DD} | {未开始/进行中/完成} |
| M1 — 设计完成 | UI/UX 设计稿交付 | {YYYY-MM-DD} | |
| M2 — 开发完成 | 全部 P0 功能可用 | {YYYY-MM-DD} | |
| M3 — 测试通过 | 完成功能测试与回归 | {YYYY-MM-DD} | |
| M4 — 上线发布 | MVP 正式发布 | {YYYY-MM-DD} | |

---

## 9. 风险与对策 (Risks & Mitigations)

| 风险描述 | 概率 | 影响程度 | 应对策略 |
|----------|------|---------|----------|
| {风险1} | {高/中/低} | {高/中/低} | {应对措施} |
| {风险2} | {高/中/低} | {高/中/低} | {应对措施} |

---

## 10. 附录 (Appendix)

### 10.1 参考文档

- {相关设计稿链接}
- {竞品分析文档链接}
- {技术架构文档链接}

### 10.2 开放问题

> 记录待决策的问题，避免遗忘。

| # | 问题描述 | 提出人 | 状态 | 结论 |
|---|---------|--------|------|------|
| 1 | {待决策问题} | {姓名} | {待讨论/已决策} | {结论} |

---

> **使用说明：**
>
> 1. 将所有 `{占位符}` 替换为实际内容
> 2. 按产品实际情况增减模块章节（第 3 节可灵活扩展）
> 3. 优先级标注（P0/P1/P2）需在需求评审会上对齐
> 4. 本模版着重于**桌面端工具类产品**，如用于移动端/Web/SaaS 等场景，请适当调整"兼容性"、"商业运营"等章节
