# API 文档模板

## 1. 接口名称

### 1.1 接口描述

{{接口用途与边界}}

### 1.2 请求方法

{{METHOD}} {{PATH}}

### 1.3 输入参数

**Path 参数**

| 参数名称 | 必选 | 类型 | 描述 |
| -------- | ---- | ---- | ---- |
| id | 是 | String | 资源 ID |

**Query 参数**

| 参数名称 | 必选 | 类型 | 描述 |
| -------- | ---- | ---- | ---- |
| page | 否 | Number | 页码 |
| page_size | 否 | Number | 每页条数 |

**Body 参数**

| 参数名称 | 必选 | 类型 | 描述 |
| -------- | ---- | ---- | ---- |
| name | 是 | String | 资源名称 |

### 1.4 输出参数

| 参数名称 | 类型 | 描述 |
| -------- | ---- | ---- |
| code | String | 业务码 |
| message | String | 返回消息 |
| data | Object | 返回数据 |
| requestId | String | 请求 ID |

### 1.5 请求示例

**输入示例**

```bash
curl -X {{METHOD}} -H'Content-Type: application/json' -H'Authorization: Bearer <token>' -d'{}' {{BASE_URL}}{{PATH}}
```

**输出示例**

```json
{
  "code": "000001",
  "message": "success",
  "data": {},
  "requestId": "req-uuid-123"
}
```

**错误返回示例**

```json
{
  "code": "040001",
  "message": "Invalid parameter",
  "data": null,
  "requestId": "req-uuid-456"
}
```

**分页返回示例**

```json
{
  "code": "000001",
  "message": "success",
  "data": {
    "page": 1,
    "page_size": 20,
    "total": 100,
    "items": []
  },
  "requestId": "req-uuid-789"
}
```

## 2. 响应格式规范

### 2.1 成功响应 (200 OK)

```json
{
  "code": "{{success_code}}",
  "message": "{{success_message}}",
  "data": {},
  "requestId": "{{request_id}}"
}
```

### 2.2 错误响应 (4xx/5xx)

```json
{
  "code": "{{error_code}}",
  "message": "{{error_message}}",
  "data": null,
  "requestId": "{{request_id}}"
}
```

### 2.3 字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `code` | string | ✅ | 业务码（格式由项目定义） |
| `message` | string | ✅ | 用户可读提示 |
| `data` | object | ❌ | 成功时返回业务数据 |
| `requestId` | string | ❌ | 请求追踪 ID |

### 2.4 国际化处理（可选）

- 语言来源：{{Accept-Language / 用户设置}}
- 回退语言：{{默认语言}}
- 翻译方式：{{本地化表 / i18n 服务}}

### 2.5 HTTP 状态码映射

| HTTP 状态码 | 适用场景 |
|-------------|----------|
| `200` | 成功 |
| `400` | 输入校验错误、业务逻辑错误 |
| `401` | 未授权、Token 失效 |
| `403` | 禁止访问、权限不足 |
| `404` | 资源不存在 |
| `409` | 状态冲突 |
| `429` | 速率限制 |
| `500` | 服务内部错误 |

## 3. 安全规范

### 3.1 Token / 认证

- 认证方式：{{Bearer Token / API Key / Session / OAuth2}}
- Token 生命周期：{{过期/刷新/撤销策略}}
- 权限模型：{{RBAC / ABAC / 自定义}}

### 3.2 请求安全

- 传输安全：{{HTTPS / mTLS / 内网}}
- CORS：{{允许来源策略}}
- 限流/防刷：{{按 IP / 用户 / Token}}

### 3.3 数据安全

- 敏感字段：{{需要脱敏/加密的字段}}
- 存储加密：{{是否加密，算法}}
- 审计日志：{{是否开启，保留时长}}

### 3.4 自定义安全条目（可扩展）

| 条目 | 内容 |
|------|------|
| {{security_item}} | {{security_detail}} |
