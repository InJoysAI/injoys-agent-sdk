# 🧾 System Module Contract

> 模块级判断合同（Owner / AI 分区）
> 用途：在不了解实现细节的情况下，依然可以验收、负责、回滚

---

## 🧩 模块基本信息

### 🧠 Owner
- 模块名称：
- 一句话角色定义（它在系统里替谁承担什么责任）：

### 🤖 AI
- 模块在系统中的位置（依赖 / 被依赖）：

---

## ① 边界（Boundary）

### 🧠 Owner（必须填写）
- ✔️ 本模块负责：
- ❌ 本模块不负责（明确拒绝）：

### 🤖 AI
- 潜在职责越界风险：
- 常见误用方式：

---

## ② 输入 / 输出契约（Contract）

### 🧠 Owner
- 输入来源：
- 非法输入期望处理方式（拒绝 / 忽略 / 记录 / 其他）：
- 输出会影响谁：

### 🤖 AI
- 输入校验与异常分类建议：
- 输出幂等性 / 重复风险说明：

---

## ③ 状态与流程（Flow）

### 🧠 Owner
- 核心流程是否能一句话描述（是 / 否）：
- 是否接受不可逆状态（是 / 否）：
- 是否接受人工介入（是 / 否）：

### 🤖 AI
- 状态枚举：
- 状态转移说明：
- 潜在卡死路径：

---

## ④ 失败定义（Failure）

### 🧠 Owner
- 最不能接受的失败（一句话）：
- 失败时偏好策略（重试 / 人工 / 中止）：

### 🤖 AI
- 失败分类（可重试 / 需人工 / 中止）：
- 极端失败场景：

---

## ⑤ 可观测性（Observability）

### 🧠 Owner
- 希望最早看到的异常信号：
- 更偏好（系统告警 / 用户感知）：

### 🤖 AI
- 核心指标（≤3）：
- 日志 / Trace 方案：

---

## ⑥ 可恢复性（Recovery）

### 🧠 Owner
- 出问题时允许的动作（停 / 回滚 / 降级 / 继续）：
- 是否接受暂时不可用（是 / 否）：

### 🤖 AI
- 恢复策略：
- Kill Switch / 降级路径：
- 副作用说明：

---

## ⑦ 可审计性（Audit）

### 🧠 Owner
- 是否必须追溯到单次行为（是 / 否）：
- 是否需要区分（输入错 / 模块错 / 假设错）：

### 🤖 AI
- Trace / ID 设计：
- 审计日志范围：

---

## ⑧ 非目标（Non-Goals）

### 🧠 Owner
- 明确不追求的目标：
- 一律拒绝的“顺手优化”：

### 🤖 AI
- 过度设计风险提示：

---

## ⑨ 风险与假设（Risks）

### 🧠 Owner
- 默认成立的关键假设：
- 最担心失效的假设：

### 🤖 AI
- 假设失效的连锁反应：
- 监控建议：

---

## ⑩ 上线 / 退出标准（Go / No-Go）

### 🧠 Owner（最终判断权）
- 最低可上线条件：
- 立即停 / 回滚信号：

### 🤖 AI
- SLO / 验收建议：
- 设计允许的失败（非 bug）：
