# {{系统名称}} 技术架构文档

**文档类型：** 技术架构设计文档 (SAD)  
**版本：** {{版本号，例如 v1.0}}  
**创建日期：** {{YYYY-MM-DD}}  
**文档状态：** {{初稿 / 评审中 / 已确认 / 已归档}}  
**关联文档：**
- 产品需求文档：`{{PRD 文档路径}}`
- 领域模型文档：`{{领域模型文档路径，可选}}`
- 数据库设计文档：`{{数据库设计文档路径，可选}}`
- API 设计文档：`{{API 设计文档路径，可选}}`

---

## 目录

1. [系统概览](#1-系统概览)
2. [服务拓扑与部署架构](#2-服务拓扑与部署架构)
3. [核心服务详细设计](#3-核心服务详细设计)
4. [数据流与调用链路](#4-数据流与调用链路)
5. [数据架构](#5-数据架构)
6. [安全架构](#6-安全架构)
7. [可观测性方案](#7-可观测性方案)
8. [技术栈清单与外部依赖](#8-技术栈清单与外部依赖)
9. [风险与缓解](#9-风险与缓解)
10. [实施与迁移路线图](#10-实施与迁移路线图)

---

## 1. 系统概览

### 1.1 产品定位

{{用 1-3 段说明系统面向的用户、核心业务场景、主要价值和边界。}}

示例：
{{系统名称}} 是面向 {{目标用户}} 的 {{产品类型}}，用于解决 {{核心问题}}。系统通过 {{核心能力}} 提供 {{关键业务价值}}，覆盖 {{主要业务流程}}。

### 1.2 核心架构理念

| 原则 | 说明 |
|------|------|
| **{{架构原则 1}}** | {{说明该原则如何影响系统拆分、调用方式、数据存储或部署模式}} |
| **{{架构原则 2}}** | {{说明该原则对应的技术取舍}} |
| **{{架构原则 3}}** | {{说明该原则对可靠性、扩展性或安全性的要求}} |
| **{{架构原则 4}}** | {{说明该原则对团队协作、演进或运维的影响}} |

### 1.3 系统上下文

```mermaid
graph TD
    subgraph 外部系统
        ExternalA[{{外部系统 A}}<br/>{{用途}}]
        ExternalB[{{外部系统 B}}<br/>{{用途}}]
    end

    subgraph 客户端
        Web[{{Web / 小程序 / App}}<br/>{{客户端说明}}]
        Admin[{{管理后台，可选}}<br/>{{后台说明}}]
    end

    subgraph 自部署服务集群
        subgraph 接入层
            Gateway[{{API Gateway / BFF}}<br/>{{鉴权 / 路由 / 聚合}}]
        end

        subgraph 业务服务层
            ServiceA[{{核心服务 A}}<br/>{{职责}}]
            ServiceB[{{核心服务 B}}<br/>{{职责}}]
        end

        subgraph 数据存储层
            DB[( {{主数据库}}<br/>{{业务数据}} )]
            Cache[( {{缓存}}<br/>{{缓存 / 限流 / 会话}} )]
            ObjectStore[( {{对象存储}}<br/>{{文件 / 媒体}} )]
        end
    end

    Web --> Gateway
    Admin --> Gateway
    Gateway --> ServiceA
    Gateway --> ServiceB
    ServiceA --> DB
    ServiceA --> Cache
    ServiceB --> DB
    ServiceB --> ObjectStore
    ServiceA --> ExternalA
    ServiceB --> ExternalB

    classDef external fill:#f9f2f4,stroke:#d6b656,stroke-width:2px,color:#333;
    classDef client fill:#d5e8d4,stroke:#82b366,stroke-width:2px,color:#333;
    classDef gateway fill:#dae8fc,stroke:#6c8ebf,stroke-width:2px,color:#333;
    classDef compute fill:#e1d5e7,stroke:#9673a6,stroke-width:2px,color:#333;
    classDef storage fill:#ffe6cc,stroke:#d79b00,stroke-width:2px,color:#333;

    class ExternalA,ExternalB external;
    class Web,Admin client;
    class Gateway gateway;
    class ServiceA,ServiceB compute;
    class DB,Cache,ObjectStore storage;
```

---

## 2. 服务拓扑与部署架构

### 2.1 部署模式

采用 **{{部署模式名称，例如：薄网关 + 业务服务 + 共享存储}}** 模式：

- **接入层**：{{说明网关、反向代理、负载均衡、TLS 终结、BFF 等职责}}
- **业务服务层**：{{说明核心服务拆分、服务边界、同步/异步调用方式}}
- **数据存储层**：{{说明数据库、缓存、对象存储、消息队列、搜索或向量库等}}
- **外部服务**：{{说明第三方 API、云服务、支付、短信、AI 模型、内容安全等}}

{{说明为什么选择该部署模式，以及它如何满足可扩展性、可靠性、成本、交付速度或合规要求。}}

### 2.2 网络拓扑

```mermaid
graph TD
    Internet((公网 Internet))

    Nginx["{{Nginx / LB / CDN}}<br/>SSL 终结 + 路由"]

    subgraph 服务集群["{{服务集群名称 / VPC / VPN / Kubernetes 集群}}"]
        subgraph 接入层
            Gateway["{{Gateway 服务名}} :{{端口}}<br/>{{运行时 / 框架}}"]
        end

        subgraph 业务服务层
            ServiceA["{{服务 A}} :{{端口}}<br/>{{职责}}"]
            ServiceB["{{服务 B}} :{{端口}}<br/>{{职责}}"]
            Worker["{{异步 Worker，可选}}<br/>{{职责}}"]
        end

        subgraph 有状态存储层
            DB["{{数据库}} :{{端口}}<br/>{{数据范围}}"]
            Cache["{{缓存}} :{{端口}}<br/>{{用途}}"]
            MQ["{{消息队列，可选}}<br/>{{用途}}"]
            ObjectStore["{{对象存储，可选}}<br/>{{用途}}"]
        end
    end

    subgraph 外部云服务["外部云服务"]
        ExternalA["{{外部服务 A}}<br/>{{用途}}"]
        ExternalB["{{外部服务 B}}<br/>{{用途}}"]
    end

    Internet -- "HTTPS 443" --> Nginx
    Nginx --> Gateway
    Gateway --> ServiceA
    Gateway --> ServiceB
    ServiceA --> DB
    ServiceA --> Cache
    ServiceB --> DB
    ServiceB --> MQ
    Worker --> MQ
    Worker --> ObjectStore
    ServiceA --> ExternalA
    ServiceB --> ExternalB

    classDef external fill:#f9f2f4,stroke:#d6b656,stroke-width:2px,color:#333;
    classDef proxy fill:#d5e8d4,stroke:#82b366,stroke-width:2px,color:#333;
    classDef gateway fill:#dae8fc,stroke:#6c8ebf,stroke-width:2px,color:#333;
    classDef compute fill:#e1d5e7,stroke:#9673a6,stroke-width:2px,color:#333;
    classDef storage fill:#ffe6cc,stroke:#d79b00,stroke-width:2px,color:#333;

    class Internet,ExternalA,ExternalB external;
    class Nginx proxy;
    class Gateway gateway;
    class ServiceA,ServiceB,Worker compute;
    class DB,Cache,MQ,ObjectStore storage;
```

> **说明**：{{说明公网入口、内网边界、服务暴露策略、东西向流量、外部云服务访问路径和安全隔离策略。}}

### 2.3 服务部署清单

| 服务 | 运行方式 | 端口 | 网络 | 扩展模式 |
|------|---------|------|------|---------|
| **{{服务 A}}** | {{Docker / Kubernetes / 进程 / 云函数}} | {{端口}} | {{公网 / 内网 / VPC / VPN}} | {{单实例 / 水平扩展 / 主从 / 分片}} |
| **{{服务 B}}** | {{运行方式}} | {{端口}} | {{网络范围}} | {{扩展模式}} |
| **{{数据库}}** | {{运行方式}} | {{端口}} | {{网络范围}} | {{单机 / 主备 / HA / 托管}} |
| **{{缓存}}** | {{运行方式}} | {{端口}} | {{网络范围}} | {{单机 / Cluster}} |

### 2.4 外部云服务清单

| 服务 | 供应商 | 用途 | 计费方式 | SLA / 风险说明 |
|------|--------|------|---------|----------------|
| **{{外部服务 A}}** | {{供应商}} | {{用途}} | {{包年包月 / 按量 / 免费额度}} | {{可用性、限流、替代方案}} |
| **{{外部服务 B}}** | {{供应商}} | {{用途}} | {{计费方式}} | {{说明}} |

### 2.5 分布式存储架构

{{说明有状态数据如何拆分：主库、缓存、对象存储、搜索、向量库、文件系统、消息队列等。明确一致性、备份、容灾、扩容和数据归属。}}

| 存储 | 数据范围 | 一致性要求 | 备份策略 | 扩展策略 |
|------|----------|------------|----------|----------|
| **{{主数据库}}** | {{核心业务数据}} | {{强一致 / 最终一致}} | {{备份频率与保留周期}} | {{读写分离 / 分区 / 分库}} |
| **{{缓存}}** | {{热点数据 / 会话 / 限流}} | {{最终一致 / 可丢失}} | {{是否需要持久化}} | {{Cluster / 扩容方式}} |
| **{{对象存储}}** | {{文件 / 图片 / 视频 / 导出物}} | {{最终一致}} | {{版本控制 / 生命周期}} | {{桶策略 / 分片上传}} |

---

## 3. 核心服务详细设计

### 3.1 {{核心服务 A 名称}}（{{服务定位}}）

#### 职责边界

- {{职责 1}}
- {{职责 2}}
- {{职责 3}}

#### 非职责

- {{不由该服务承担的职责 1}}
- {{不由该服务承担的职责 2}}

#### 关键接口

| 方法 | 路径 / Topic | 用途 | 认证 | 备注 |
|------|--------------|------|------|------|
| `{{GET/POST}}` | `{{/api/path}}` | {{用途}} | {{认证方式}} | {{备注}} |

#### 内部模块

| 模块 | 职责 | 关键依赖 |
|------|------|----------|
| {{模块 A}} | {{职责}} | {{依赖}} |
| {{模块 B}} | {{职责}} | {{依赖}} |

#### 关键设计决策

- **{{决策点 1}}**：{{说明方案、理由、替代方案和影响。}}
- **{{决策点 2}}**：{{说明方案、理由、替代方案和影响。}}

### 3.2 {{核心服务 B 名称}}（{{服务定位}}）

参照 3.1 补充职责边界、接口、内部模块和关键设计决策。

### 3.3 {{前端 / 客户端名称}}（{{客户端类型}}）

#### 职责边界

- {{用户界面与交互职责}}
- {{本地状态、缓存或离线能力}}
- {{与后端 API 的交互方式}}

#### 页面 / 模块清单

| 页面 / 模块 | 职责 | 依赖接口 | 权限要求 |
|-------------|------|----------|----------|
| {{页面 A}} | {{职责}} | {{接口}} | {{权限}} |

### 3.4 质量保障技术方案

| 质量目标 | 技术方案 | 验收方式 |
|----------|----------|----------|
| {{性能}} | {{缓存、批处理、异步化、索引等}} | {{压测 / 指标阈值}} |
| {{可靠性}} | {{重试、熔断、降级、幂等等}} | {{故障演练 / 单测 / 集成测试}} |
| {{安全性}} | {{鉴权、加密、审计、脱敏等}} | {{安全测试 / 权限矩阵}} |

---

## 4. 数据流与调用链路

### 4.1 {{流程 A 名称，例如：用户登录流程}}

```mermaid
sequenceDiagram
    participant U as 用户
    participant C as 客户端
    participant G as Gateway
    participant S as {{服务 A}}
    participant DB as 数据库

    U->>C: {{用户操作}}
    C->>G: {{请求}}
    G->>S: {{转发 / 调用}}
    S->>DB: {{读写数据}}
    DB-->>S: {{返回}}
    S-->>G: {{返回}}
    G-->>C: {{响应}}
    C-->>U: {{展示结果}}
```

### 4.2 {{流程 B 名称，例如：核心业务请求全链路}}

```mermaid
sequenceDiagram
    participant C as 客户端
    participant G as Gateway
    participant A as {{核心服务 A}}
    participant B as {{核心服务 B}}
    participant E as {{外部服务}}

    C->>G: {{业务请求}}
    G->>A: {{鉴权后调用}}
    A->>B: {{内部调用}}
    B->>E: {{外部 API 调用}}
    E-->>B: {{外部响应}}
    B-->>A: {{处理结果}}
    A-->>G: {{业务响应}}
    G-->>C: {{最终响应}}
```

### 4.3 {{流程 C 名称，例如：异步任务处理流程}}

{{说明消息生产、投递、消费、幂等、重试、死信队列和人工补偿方式。}}

### 4.4 外部服务调用统一链路

{{说明外部服务调用的入口、鉴权、超时、重试、限流、熔断、审计、成本控制和降级策略。}}

---

## 5. 数据架构

### 5.1 数据库分布

| 数据库 / Schema | 所属服务 | 数据范围 | 读写模式 |
|-----------------|----------|----------|----------|
| {{数据库 A}} | {{服务 A}} | {{数据范围}} | {{读写 / 只读 / 主从}} |
| {{数据库 B}} | {{服务 B}} | {{数据范围}} | {{读写模式}} |

### 5.2 关键表设计

#### {{表名 A}}

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | `{{类型}}` | `PRIMARY KEY` | {{主键说明}} |
| `{{字段}}` | `{{类型}}` | `{{约束}}` | {{说明}} |
| `created_at` | `{{类型}}` | `NOT NULL` | 创建时间 |
| `updated_at` | `{{类型}}` | `NOT NULL` | 更新时间 |

#### {{表名 B}}

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | `{{类型}}` | `PRIMARY KEY` | {{主键说明}} |

### 5.3 缓存使用方案

| Key / Namespace | 数据内容 | TTL | 失效策略 | 所属服务 |
|-----------------|----------|-----|----------|----------|
| `{{cache:key}}` | {{内容}} | {{TTL}} | {{主动失效 / 自然过期}} | {{服务}} |

### 5.4 搜索 / 向量 / 索引存储

| 存储 | 数据内容 | 索引策略 | 更新方式 | 查询场景 |
|------|----------|----------|----------|----------|
| {{搜索引擎 / 向量库}} | {{内容}} | {{索引 / embedding / 分词}} | {{同步 / 异步}} | {{场景}} |

### 5.5 数据库迁移策略

- **Schema 管理**：{{迁移工具、SSoT 目录、版本命名规则}}
- **兼容策略**：{{expand-contract、灰度发布、双写、回填}}
- **回滚策略**：{{回滚脚本、备份恢复、开关降级}}
- **数据校验**：{{迁移前后校验、抽样、对账}}

### 5.6 数据保留与归档

| 数据类型 | 保留周期 | 归档方式 | 删除策略 | 合规说明 |
|----------|----------|----------|----------|----------|
| {{数据类型}} | {{周期}} | {{归档位置}} | {{软删 / 硬删}} | {{合规要求}} |

---

## 6. 安全架构

### 6.1 认证与授权

| 场景 | 认证方式 | 授权模型 | Token / Session 策略 |
|------|----------|----------|----------------------|
| {{用户端}} | {{JWT / OAuth / Session}} | {{RBAC / ABAC / ACL}} | {{有效期、刷新、吊销}} |
| {{管理端}} | {{认证方式}} | {{授权模型}} | {{策略}} |
| {{服务间调用}} | {{mTLS / 内网 Token / IAM}} | {{最小权限}} | {{轮换策略}} |

### 6.2 内容安全与输入校验

- **输入校验**：{{参数校验、Schema 校验、文件类型限制、大小限制}}
- **内容安全**：{{敏感内容审核、反垃圾、风控策略}}
- **输出保护**：{{敏感字段脱敏、响应过滤、防泄漏}}

### 6.3 API 安全

- **限流**：{{按用户、IP、租户、接口维度的限流策略}}
- **防重放**：{{签名、时间戳、Nonce、幂等键}}
- **CORS / CSRF**：{{浏览器端安全策略}}
- **审计**：{{关键操作审计字段、保留周期、查询方式}}

### 6.4 数据安全

- **传输加密**：{{TLS 版本、证书管理}}
- **存储加密**：{{数据库、对象存储、备份加密}}
- **密钥管理**：{{KMS / Secret Manager / 环境变量 / 轮换策略}}
- **隐私保护**：{{PII 分类、脱敏、最小化采集、删除请求}}

### 6.5 业务安全与滥用防护

{{说明刷量、越权、资源滥用、恶意文件、Prompt 注入、模型越狱或其他业务特有风险的防护策略。}}

---

## 7. 可观测性方案

### 7.0 MVP 最小日志方案（必须）

| 日志类型 | 必填字段 | 用途 |
|----------|----------|------|
| 请求日志 | `request_id`, `user_id`, `method`, `path`, `status`, `latency_ms` | 排障与审计 |
| 错误日志 | `request_id`, `error_code`, `error_message`, `stack`, `service` | 错误定位 |
| 业务日志 | `request_id`, `event`, `entity_id`, `actor_id`, `result` | 业务追踪 |

### 7.1 日志采集

- **日志格式**：{{JSON / text，字段规范}}
- **采集链路**：{{stdout -> agent -> 日志平台}}
- **保留周期**：{{线上 / 测试环境日志保留周期}}
- **敏感信息处理**：{{脱敏字段、禁止记录内容}}

### 7.2 指标监控

| 指标 | 类型 | 标签 | 告警阈值 |
|------|------|------|----------|
| `http_requests_total` | Counter | `service`, `path`, `status` | {{阈值}} |
| `http_request_duration_seconds` | Histogram | `service`, `path` | P95/P99 {{阈值}} |
| `{{业务指标}}` | {{Counter/Gauge/Histogram}} | {{标签}} | {{阈值}} |

### 7.3 告警通知

| 告警项 | 触发条件 | 级别 | 处理人 | Runbook |
|--------|----------|------|--------|---------|
| {{服务不可用}} | {{连续 N 分钟错误率超过阈值}} | P0/P1 | {{团队/人员}} | `{{链接}}` |
| {{外部依赖异常}} | {{超时率 / 错误率}} | P1/P2 | {{团队/人员}} | `{{链接}}` |

### 7.4 分布式链路追踪（可选）

{{说明 Trace ID 传递、采样率、OpenTelemetry 接入、跨服务调用 span 规范和查询方式。}}

---

## 8. 技术栈清单与外部依赖

### 8.1 自建服务技术栈

| 服务 | 语言 / 框架 | 关键库 | 构建方式 | 备注 |
|------|-------------|--------|----------|------|
| {{服务 A}} | {{语言 / 框架}} | {{关键库}} | {{Dockerfile / CI}} | {{备注}} |
| {{服务 B}} | {{语言 / 框架}} | {{关键库}} | {{构建方式}} | {{备注}} |

### 8.2 基础设施

| 类型 | 技术选型 | 用途 | 备注 |
|------|----------|------|------|
| 反向代理 / LB | {{Nginx / ALB / Ingress}} | {{用途}} | {{备注}} |
| 数据库 | {{PostgreSQL / MySQL / MongoDB}} | {{用途}} | {{备注}} |
| 缓存 | {{Redis / Memcached}} | {{用途}} | {{备注}} |
| 消息队列 | {{Kafka / RabbitMQ / SQS}} | {{用途}} | {{备注}} |
| 对象存储 | {{S3 / OSS / COS / MinIO}} | {{用途}} | {{备注}} |

### 8.3 外部服务 API 依赖

| 外部服务 | 调用方 | 协议 | 超时 | 重试 | 降级策略 |
|----------|--------|------|------|------|----------|
| {{服务 A}} | {{调用服务}} | {{HTTPS / gRPC}} | {{超时}} | {{重试次数}} | {{降级策略}} |
| {{服务 B}} | {{调用服务}} | {{协议}} | {{超时}} | {{重试}} | {{降级策略}} |

---

## 9. 风险与缓解

### 9.1 技术风险

| 风险 | 影响 | 概率 | 缓解措施 | 负责人 |
|------|------|------|----------|--------|
| {{技术风险 A}} | {{影响范围}} | {{高/中/低}} | {{缓解措施}} | {{负责人}} |
| {{技术风险 B}} | {{影响范围}} | {{高/中/低}} | {{缓解措施}} | {{负责人}} |

### 9.2 运维风险

| 风险 | 影响 | 概率 | 缓解措施 | 负责人 |
|------|------|------|----------|--------|
| {{运维风险 A}} | {{影响范围}} | {{高/中/低}} | {{缓解措施}} | {{负责人}} |

### 9.3 业务风险

| 风险 | 影响 | 概率 | 缓解措施 | 负责人 |
|------|------|------|----------|--------|
| {{业务风险 A}} | {{影响范围}} | {{高/中/低}} | {{缓解措施}} | {{负责人}} |

---

## 10. 实施与迁移路线图

### 10.1 阶段总览

| 阶段 | 目标 | 主要工作 | 验收标准 |
|------|------|----------|----------|
| Phase 0：准备 | {{目标}} | {{主要工作}} | {{验收标准}} |
| Phase 1：MVP | {{目标}} | {{主要工作}} | {{验收标准}} |
| Phase 2：增强 | {{目标}} | {{主要工作}} | {{验收标准}} |
| Phase 3：规模化 | {{目标}} | {{主要工作}} | {{验收标准}} |

### 10.2 详细计划

#### Phase 0：{{阶段名称}}

- [ ] {{任务 1}}
- [ ] {{任务 2}}
- [ ] {{任务 3}}

#### Phase 1：{{阶段名称}}

- [ ] {{任务 1}}
- [ ] {{任务 2}}
- [ ] {{任务 3}}

#### Phase 2：{{阶段名称}}

- [ ] {{任务 1}}
- [ ] {{任务 2}}
- [ ] {{任务 3}}

### 10.3 兼容与废弃清单

| 项目 | 当前状态 | 目标状态 | 处理方式 | 截止时间 |
|------|----------|----------|----------|----------|
| {{旧接口 / 旧模块 / 旧表}} | {{当前状态}} | {{目标状态}} | {{保留 / 替换 / 删除}} | {{日期}} |

---

## 附录

### A. 环境变量清单

| 变量名 | 所属服务 | 必填 | 示例 | 说明 |
|--------|----------|------|------|------|
| `{{ENV_NAME}}` | {{服务}} | 是/否 | `{{示例}}` | {{说明}} |

### B. API 端点清单

| 方法 | 路径 | 所属服务 | 认证 | 说明 |
|------|------|----------|------|------|
| `GET` | `{{/api/path}}` | {{服务}} | {{认证}} | {{说明}} |
| `POST` | `{{/api/path}}` | {{服务}} | {{认证}} | {{说明}} |

### C. 业务错误码体系

| 错误码 | HTTP 状态码 | 说明 | 用户提示 | 处理方式 |
|--------|-------------|------|----------|----------|
| `{{ERR_CODE}}` | {{状态码}} | {{说明}} | {{提示}} | {{处理方式}} |

### D. 术语表

| 术语 | 说明 |
|------|------|
| {{术语 A}} | {{说明}} |
| {{术语 B}} | {{说明}} |
